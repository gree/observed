require 'observed/builtin_plugins'
require 'observed/http'

observe 'foo_1', {
  plugin: 'http',
  method: 'get',
  url: 'http://google.com',
  timeout_in_milliseconds: 1000
}

match 'foo_*', {
  plugin: 'stdout'
}
